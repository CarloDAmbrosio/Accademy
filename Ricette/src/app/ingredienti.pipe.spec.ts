import { IngredientiPipe } from './ingredienti.pipe';

describe('IngredientiPipe', () => {
  it('create an instance', () => {
    const pipe = new IngredientiPipe();
    expect(pipe).toBeTruthy();
  });
});
