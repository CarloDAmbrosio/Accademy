import { IngredientiNonPresentiPipe } from './ingredienti-non-presenti.pipe';

describe('IngredientiNonPresentiPipe', () => {
  it('create an instance', () => {
    const pipe = new IngredientiNonPresentiPipe();
    expect(pipe).toBeTruthy();
  });
});
